<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Kept explicit for a future secure-session store; app backups are disabled in the manifest. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
